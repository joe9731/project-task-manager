{
  "name": "project-task-manager",
  "version": "0.0.1",
  "description": "Project-level task management plugin for Obsidian — scaffold",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "node -r ts-node/register scripts/run-parser-tests.ts"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "moment": "^2.29.4",
    "chrono-node": "^2.3.1"
  }
}
